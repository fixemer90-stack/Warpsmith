---
title: "F2.8 — Victory Points Tracking"
parent: phase2-game-system
status: done
priority: 3
---

# F2.8: Victory Points Tracking and End-Game Conditions

**Файл:** `backend/state/mission.py` (расширение)
**Зависимости:** F2.4 (Missions), F2.5 (Game Loop)
**Часы:** 2h

**Цель (SMART):** Реализовать систему подсчёта VP каждый раунд, проверку end-game условий и определение победителя. Критерий: после 5 раундов определяется победитель по VP; игра заканчивается досрочно при wipe армии или достижении 100 VP; подсчёт поддерживает 3 режима (standard, progressive, kill_points). 2 часа.

---

## VP Tracker

```python
from dataclasses import dataclass, field

@dataclass
class VPTracker:
    """Подсчёт VP на протяжении игры."""
    history: dict[int, list[int]] = field(default_factory=lambda: {1: [], 2: []})
    total: dict[int, int] = field(default_factory=lambda: {1: 0, 2: 0})

    def add(self, player: int, vp: int) -> None:
        self.history[player].append(vp)
        self.total[player] += vp

    def round_vp(self, player: int, round_num: int) -> int:
        """VP полученные в конкретном раунде."""
        if round_num <= len(self.history[player]):
            return self.history[player][round_num - 1]
        return 0

    def leader(self) -> int:
        """Текущий лидер по VP."""
        return 1 if self.total[1] >= self.total[2] else 2

    def is_tied(self) -> bool:
        return self.total[1] == self.total[2]

    def margin(self) -> int:
        return abs(self.total[1] - self.total[2])

    def summary(self) -> dict:
        return {
            "player_1": {"total": self.total[1], "per_round": self.history[1]},
            "player_2": {"total": self.total[2], "per_round": self.history[2]},
            "winner": self.leader() if not self.is_tied() else "tie",
            "margin": self.margin(),
        }
```

## End-Game Conditions

```python
@dataclass
class GameResult:
    winner: Optional[int]       # None = tie
    reason: str                 # "rounds_completed", "army_wiped", "vp_cap"
    vp_tracker: VPTracker
    total_rounds: int
    summary: dict

def check_end_game(state: GameState, mission: Mission,
                   vp: VPTracker, round_num: int) -> Optional[GameResult]:
    """Проверить условия окончания игры."""

    # 1. Victory Point cap (100 VP)
    for player in [1, 2]:
        if vp.total[player] >= 100:
            return GameResult(
                winner=player,
                reason="vp_cap",
                vp_tracker=vp,
                total_rounds=round_num,
                summary=vp.summary(),
            )

    # 2. Army wiped
    for player, roster in [(1, state.roster_a), (2, state.roster_b)]:
        if all(u.squad_size <= 0 for u in roster.units):
            winner = 2 if player == 1 else 1
            return GameResult(
                winner=winner,
                reason="army_wiped",
                vp_tracker=vp,
                total_rounds=round_num,
                summary=vp.summary(),
            )

    # 3. Max rounds reached
    if round_num >= mission.config.max_rounds:
        leader = vp.leader()
        if vp.is_tied():
            # Tie-breakers:
            # 1. Who killed more pts?
            # 2. Who has more objectives?
            # 3. Random
            leader = _resolve_tie(state)
        return GameResult(
            winner=leader if not vp.is_tied() else None,
            reason="rounds_completed",
            vp_tracker=vp,
            total_rounds=round_num,
            summary=vp.summary(),
        )

    return None  # game continues


def _resolve_tie(state: GameState) -> int:
    """Tie-break: больше убитых очков -> больше контролируемых точек -> random."""
    killed_a = _calc_killed_pts(state.roster_a, state)
    killed_b = _calc_killed_pts(state.roster_b, state)
    if killed_a != killed_b:
        return 1 if killed_a > killed_b else 2
    # TODO: count controlled objectives
    return 1  # fallback: P1 wins
```

## Monthly Scoring (Legacy)

```python
SCORING_MAP = {
    "standard": score_standard,
    "progressive": score_progressive,
    "kill_points": score_kill_points,
}

def apply_scoring(state: GameState, mission: Mission, vp: VPTracker) -> VPTracker:
    """Подсчитать VP за текущий раунд, добавить в трекер."""
    scorer = SCORING_MAP.get(mission.config.scoring_rule, score_standard)
    round_vp = scorer(mission)
    for player in [1, 2]:
        vp.add(player, round_vp[player])
    return vp
```

## Тесты

```python
def test_vp_tracker():
    vp = VPTracker()
    vp.add(1, 10)
    vp.add(2, 5)
    vp.add(1, 5)
    assert vp.total[1] == 15
    assert vp.total[2] == 5
    assert vp.leader() == 1

def test_vp_cap():
    vp = VPTracker()
    vp.add(1, 100)
    state = GameState(...)
    result = check_end_game(state, _only_war_mission(), vp, 3)
    assert result is not None
    assert result.reason == "vp_cap"
    assert result.winner == 1

def test_game_continues():
    vp = VPTracker()
    vp.add(1, 30)
    vp.add(2, 25)
    state = GameState(roster_a=RosterState(units=[UnitState(squad_size=5)]), ...)
    result = check_end_game(state, _only_war_mission(), vp, 3)
    assert result is None  # game continues

def test_scoring_map():
    assert "standard" in SCORING_MAP
    assert "progressive" in SCORING_MAP
    assert "kill_points" in SCORING_MAP
```
