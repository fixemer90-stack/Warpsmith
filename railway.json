{
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT --no-reload",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}
