---
title: "F5.2 — Deployment (Dokku / Railway / self-host)"
parent: phase5-production
status: pending
priority: 1
---

# F5.2: Deployment — Dokku / Railway / self-host
**Цель (SMART):** Развернуть приложение на production-сервере с HTTPS, БД и автодеплоем. Критерий: curl https://<domain>/ → 200, регистрация/логин работают, SQLite не теряет данные при рестарте. Документация деплоя покрывает 3 сценария: Dokku, Railway, bare-metal (systemd). 4 часа.

**Файлы:**
- `Procfile` (создать — Railway/Dokku)
- `app.json` (создать — Dokku)
- `deploy/` директория:
  - `deploy/dokku-setup.sh` (создать)
  - `deploy/railway.json` (создать)
  - `deploy/systemd.service` (создать)
- `docs/deployment.md` (создать)

**Зависимости:** F5.1 (Dockerfile)
**Часы:** 4h

---

## Procfile (корень проекта)

```
web: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}
```

## app.json (Dokku)

```json
{
  "name": "warpsmith",
  "description": "WH40k Battle Simulator",
  "scripts": {
    "dokku": {
      "predeploy": "python -c 'from backend.db.database import db; db.migrate()'"
    }
  },
  "env": {
    "HOSTING": { "value": "true" },
    "DB_PATH": { "value": "/data/simulator.db" }
  }
}
```

## Dokku setup скрипт

```bash
#!/usr/bin/env bash
# deploy/dokku-setup.sh — настроить Dokku-приложение
set -euo pipefail

APP="warpsmith"
DOMAIN="${1:-example.com}"

# Создать приложение
dokku apps:create "$APP" || true

# Установить домен и SSL
dokku domains:set "$APP" "$DOMAIN"
dokku letsencrypt:enable "$APP"

# Подключить volume для SQLite
dokku storage:ensure-directory "$APP-data"
dokku storage:mount "$APP" /var/lib/dokku/data/storage/"$APP-data":/data

# Переменные окружения
dokku config:set "$APP" HOSTING=true DB_PATH=/data/simulator.db

# Dockerfile деплой
dokku builder:set "$APP" selected dockerfile

echo "✓ Готово. git push dokku main для деплоя."
```

## Railway конфиг

```json
// deploy/railway.json
{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
```

## systemd service (bare-metal)

```ini
[Unit]
Description=Warpsmith Battle Simulator
After=network.target

[Service]
Type=simple
User=warpsmith
WorkingDirectory=/opt/warpsmith
Environment=HOSTING=true
Environment=DB_PATH=/var/lib/warpsmith/simulator.db
Environment=PORT=8000
ExecStart=/opt/warpsmith/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

## docs/deployment.md содержание

Документ описывает:
1. **Dokku** — `git push`-деплой, letsencrypt, volume mount
2. **Railway** — connect GitHub repo, Dockerfile build, env vars
3. **Self-host (bare-metal)** — python venv, systemd, nginx reverse proxy + certbot
4. **Переменные окружения** — полный список с описанием
5. **Healthcheck и мониторинг** — uptime check, логи

## Переменные окружения (production)

| Переменная | Значение | Где задаётся |
|------------|----------|-------------|
| `HOSTING` | `true` | Всегда в production |
| `DB_PATH` | `/data/simulator.db` | Для volume-mounted SQLite |
| `ALLOWED_ORIGINS` | `https://ваш-домен.com` | Ограничить CORS |
| `JWT_SECRET` | `<random-64-char>` | Обязательно сменить |
| `PORT` | `8000` | Railway/докер задают авто |

## Безопасность

- JWT_SECRET генерируется через `openssl rand -hex 32`
- HTTPS настроен через letsencrypt (Dokku) или nginx (self-host)
- Production не использует reload
- SQLite volume монтируется отдельно, не в слое контейнера

## Тесты

```python
def test_procfile_exists():
    assert Path("Procfile").exists()

def test_production_env_vars():
    """Проверка, что все production-переменные документированы."""
    ...
```
