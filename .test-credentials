# Test user credentials — локальная разработка
# Не коммитить! Добавлено в .gitignore.

Railway-деплой (warpsmith-production.up.railway.app):
  email:   test@warpsmith.test
  pass:    TestPass123!
  display: TestUser
  note:    Создан 2026-05-04, может быть удалён при сбросе БД на Railway

Локально — создаётся через register, хранится в *.db (.gitignore)
