---
title: "F2.4 — Missions"
parent: phase2-game-system
status: done
priority: 2
---

# F2.4: Missions — Objectives, Scoring, Deployment Rules

**Файл:** `backend/state/mission.py`
**Зависимости:** F2.1 (Game State), F2.2 (Map)
**Часы:** 3h

**Цель (SMART):** Реализовать систему миссий с правилами высадки, условиями захвата точек и подсчётом VP. Критерий: 3 готовые миссии (Only War, Purge the Foe, Take and Hold), каждая задаёт deploy зоны, 3-5 objectives, scoring rule, подсчёт VP через `calculate_victory_points()` на фазе Command. 3 часа.

---

## Data Model

```python
class MissionObjective:
    x: int; y: int; label: str
    controlled_by: str | None    # player_id
    is_contested: bool

@dataclass
class MissionConfig:
    name: str
    deployment: DeploymentType
    description: str
    objectives: list[MissionObjective]
    max_rounds: int = 5
    scoring_rule: str = "standard"  # standard | progressive | kill_points

@dataclass
class Mission:
    config: MissionConfig
    state: GameState
```

## Scoring — calculate_victory_points()

Единый метод на Mission, вызывается из `_command_phase()` каждый раунд.
Внутри диспатчит по `config.scoring_rule`:

| scoring_rule | Логика |
|---|---|
| `standard` | VP = число контролируемых objectives (1 VP за каждый) |
| `progressive` | VP = objectives controlled + бонус +2 за контроль больше чем оппонент |
| `kill_points` | VP = % уничтоженных очков армии противника × 100 |

```python
def calculate_victory_points(self) -> dict[str, int]:
    self.update_objective_control()
    # ... dispatch by scoring_rule
    return {player_id: vp, ...}
```

## Built-in Missions

| Миссия | Deploy | Objectives | Scoring |
|--------|--------|-----------|---------|
| Only War | Dawn of War | 3 (Center/Mid Left/Mid Right) | progressive |
| Purge the Foe | Search & Destroy | 0 | kill_points |
| Take and Hold | Crucible of Battle | 5 | standard |

## Вызов из Game Loop

`_command_phase()` в `backend/engine/scenario.py`:

```python
if self.state.mission:
    vp_awarded = self.state.mission.calculate_victory_points()
    for player_id, vp in vp_awarded.items():
        self.state.players[player_id].victory_points += vp
```

VP суммируются в `PlayerState.victory_points` каждый раунд.
Конец игры — по раундам (`current_round > max_rounds`) или VP ≥ 10 (F2.8).

## Тесты

- `test_update_objective_control` — юнит на точке → controlled_by
- `test_progressive_scoring` — бонус +2 за большинство objectives
- `test_create_mission_function` — 3 миссии создаются через реестр
- `test_mission_registry` — MISSIONS содержит все 3 конфига
