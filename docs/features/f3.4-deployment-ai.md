---
title: "F3.4 — Deployment AI"
parent: phase3-ai-automation
status: done
priority: 3
---

# F3.4: Deployment AI — Zone Placement Logic

**Файл:** `backend/engine/ai/deployment.py` (создать)
**Зависимости:** F2.2 (2D Map), F2.4 (Missions), F2.1 (Game State)
**Часы:** 3h

**Цель (SMART):** Реализовать DeploymentAI, размещающий юниты в deploy zone согласно миссии, с поддержкой: стандартной расстановки (12" от центра), Hammer & Anvil (длинные стороны), Search & Destroy (углы), Dawn of War (полоски). Критерий: все 4 deployment типа размещают юниты в корректных deploy zone (минимум 8 тестов), юниты не накладываются друг на друга и не выходят за границы карты. 3 часа.

---

## Deployment Types

```python
from enum import Enum
from dataclasses import dataclass
from typing import Optional


class DeploymentType(Enum):
    STANDARD = "standard"             # 12" от центра
    HAMMER_AND_ANVIL = "hammer_and_anvil"  # длинные стороны
    SEARCH_AND_DESTROY = "search_and_destroy"  # углы
    DAWN_OF_WAR = "dawn_of_war"       # полоски


@dataclass
class DeploymentZone:
    """Прямоугольная зона развёртывания."""
    player: int
    x_min: int
    x_max: int
    y_min: int
    y_max: int
    label: str = ""
```

## Zone Calculation

```python
def get_deployment_zone(deployment_type: DeploymentType,
                        player: int,
                        map_size: tuple[int, int]) -> DeploymentZone:
    """Вернуть deploy zone для игрока на карте map_size (width, height).

    STANDARD:
      P1: x ∈ [0, width/3], P2: x ∈ [2*width/3, width]

    HAMMER_AND_ANVIL:
      P1: x ∈ [0, width/4], P2: x ∈ [3*width/4, width]

    SEARCH_AND_DESTROY:
      P1: угол (0, 0) — (width/3, height/3)
      P2: угол (2*width/3, 2*height/3) — (width, height)

    DAWN_OF_WAR:
      P1: x ∈ [0, width/2], y ∈ [0, height/4]
      P2: x ∈ [0, width/2], y ∈ [3*height/4, height]
    """
    w, h = map_size

    if deployment_type == DeploymentType.STANDARD:
        if player == 1:
            return DeploymentZone(1, 0, w // 3, 0, h, "P1 Standard")
        else:
            return DeploymentZone(2, 2 * w // 3, w, 0, h, "P2 Standard")

    elif deployment_type == DeploymentType.HAMMER_AND_ANVIL:
        if player == 1:
            return DeploymentZone(1, 0, w // 4, 0, h, "P1 Hammer")
        else:
            return DeploymentZone(2, 3 * w // 4, w, 0, h, "P2 Anvil")

    elif deployment_type == DeploymentType.SEARCH_AND_DESTROY:
        if player == 1:
            return DeploymentZone(1, 0, w // 3, 0, h // 3, "P1 S&D")
        else:
            return DeploymentZone(2, 2 * w // 3, w, 2 * h // 3, h, "P2 S&D")

    elif deployment_type == DeploymentType.DAWN_OF_WAR:
        if player == 1:
            return DeploymentZone(1, 0, w // 2, 0, h // 4, "P1 DoW")
        else:
            return DeploymentZone(2, 0, w // 2, 3 * h // 4, h, "P2 DoW")

    raise ValueError(f"Unknown deployment type: {deployment_type}")
```

## Placement Strategy

```python
@dataclass
class Placement:
    """Результат размещения одного юнита."""
    unit_id: str
    x: int
    y: int
    is_in_cover: bool = False
    is_on_objective: bool = False


def place_units(
    player_units: list[UnitState],
    unit_models: dict[str, Unit] | None = None,
    deployment_type: DeploymentType = DeploymentType.STANDARD,
    player: int = 1,
    map_size: tuple[int, int] = (60, 44),
    battlefield: BattlefieldMap | None = None,
    objectives: list[MissionObjective] | None = None,
    warlord_id: str | None = None,
) -> list[Placement]:
    """Разместить все юниты в deploy zone.

    Стратегия размещения:
    1. Warlord → центр зоны (лидерский бонус)
    2. Melee units → передний край deploy zone
    3. Ranged units → задний край / в cover
    4. Epic Heroes → рядом с подходящим отрядом
    5. Транспорты → вместить отряды, поставить в cover
    """
    zone = get_deployment_zone(deployment_type, player, map_size)
    sorted_units = _sort_units_by_role(player_units, unit_models, warlord_id)
    occupied: set[tuple[int, int]] = set()
    placements: list[Placement] = []
    is_p1 = player == 1

    for unit in sorted_units:
        model = unit_models.get(unit.unit_id) if unit_models else None
        pos = _find_best_position(
            unit,
            zone,
            battlefield,
            objectives or [],
            occupied,
            model,
            warlord_id,
            is_p1,
        )
        if pos is not None:
            placements.append(Placement(
                unit_id=unit.unit_id,
                x=pos[0], y=pos[1],
                is_in_cover=_has_cover_at(battlefield, pos[0], pos[1]),
                is_on_objective=_is_on_objective(pos, objectives or []),
            ))
            occupied.add(pos)

    return placements


def _sort_units_by_role(
    units: list[UnitState],
    unit_models: dict[str, Unit] | None = None,
    warlord_id: str | None = None,
) -> list[UnitState]:
    """Сортировать: Warlord → Transport → Melee → Battleline → Ranged → Support."""
    def sort_key(unit: UnitState) -> int:
        model = unit_models.get(unit.unit_id) if unit_models else None
        if warlord_id and unit.unit_id == warlord_id:
            return 0
        keywords = getattr(unit, "keywords", [])
        if isinstance(keywords, (list, tuple)) and "transport" in keywords:
            return 1
        if _is_melee_unit(unit, model):
            return 2
        if isinstance(keywords, (list, tuple)) and "battleline" in keywords:
            return 3
        if _is_ranged_unit(unit, model):
            return 4
        return 5

    return sorted(units, key=sort_key)


def _find_best_position(
    unit: UnitState,
    zone: DeploymentZone,
    battlefield: BattlefieldMap | None,
    objectives: list[MissionObjective],
    occupied: set[tuple[int, int]],
    unit_model: Unit | None = None,
    warlord_id: str | None = None,
    is_p1: bool = True,
) -> tuple[int, int] | None:
    """Найти лучшую позицию для юнита в зоне.

    - Не занято другим юнитом (margin 2 клетки)
    - Приоритет cover (если ranged)
    - Приоритет objective (если melee с высоким OC)
    - Не ближе 3" от другого юнита (coherency)
    """
    best_score = -1.0
    best_pos: tuple[int, int] | None = None

    front_edge_x = zone.x_max if is_p1 else zone.x_min
    center = (
        (zone.x_min + zone.x_max) // 2,
        (zone.y_min + zone.y_max) // 2,
    )

    for x in range(zone.x_min, zone.x_max + 1):
        for y in range(zone.y_min, zone.y_max + 1):
            if _is_occupied(x, y, occupied, margin=2):
                continue
            if not _is_walkable(battlefield, x, y):
                continue

            score = 0.0

            if _is_ranged_unit(unit, unit_model) and _has_cover_at(battlefield, x, y):
                score += 0.5

            if _is_melee_unit(unit, unit_model):
                dist_to_front = abs(x - front_edge_x)
                max_depth = abs(zone.x_max - zone.x_min) or 1
                score += 0.8 * (1.0 - dist_to_front / max_depth)
                if _is_on_objective((x, y), objectives):
                    score += 0.8

            if warlord_id and unit.unit_id == warlord_id:
                dist_center = _distance((x, y), center)
                score += 0.3 * max(0.0, 1.0 - dist_center / 20.0)

            if score > best_score:
                best_score = score
                best_pos = (x, y)

    return best_pos or _find_first_free(zone, occupied)
```

## Edge Cases

| Edge Case | Ожидаемое поведение |
|-----------|---------------------|
| Deploy zone полностью занята террейном | Использовать ближайшую свободную клетку |
| Больше юнитов чем свободных клеток | Разместить вплотную (min coherency 1") |
| Юнит не влезает в deploy zone (большая база) | Центрировать, проверить все 4 угла базы |
| Нет objectives на карте | Разместить равномерно по deploy zone |
| Транспорт с embarked отрядами | Разместить транспорт, отряды внутри |
| Warlord уничтожен до размещения | Пропустить warlord sorting |
| Тип deployment неизвестен | По умолчанию STANDARD |
| Карта нестандартного размера | Зоны пропорциональны размеру карты |

## Тесты

```python
def test_standard_deployment_p1():
    zone = get_deployment_zone(DeploymentType.STANDARD, 1, (60, 44))
    assert zone.x_min == 0
    assert zone.x_max == 20  # 60 // 3
    assert zone.y_min == 0
    assert zone.y_max == 44


def test_standard_deployment_p2():
    zone = get_deployment_zone(DeploymentType.STANDARD, 2, (60, 44))
    assert zone.x_min == 40  # 2 * 60 // 3
    assert zone.x_max == 60
    assert zone.y_min == 0
    assert zone.y_max == 44


def test_hammer_and_anvil():
    zone = get_deployment_zone(DeploymentType.HAMMER_AND_ANVIL, 1, (60, 44))
    assert zone.x_max == 15  # 60 // 4


def test_search_and_destroy_p1():
    zone = get_deployment_zone(DeploymentType.SEARCH_AND_DESTROY, 1, (60, 44))
    assert zone.x_max == 20   # 60 // 3
    assert zone.y_max == 14   # 44 // 3


def test_dawn_of_war():
    zone = get_deployment_zone(DeploymentType.DAWN_OF_WAR, 1, (60, 44))
    assert zone.y_max == 11  # 44 // 4


def test_dawn_of_war_p2():
    zone = get_deployment_zone(DeploymentType.DAWN_OF_WAR, 2, (60, 44))
    assert zone.y_min == 33  # 3 * 44 // 4


def test_place_units_no_overlap():
    player_units = make_unit_states(count=5)
    placements = place_units(player_units, deployment_type=DeploymentType.STANDARD,
                             map_size=(60, 44))
    positions = [(p.x, p.y) for p in placements]
    assert len(positions) == len(set(positions))


def test_place_units_in_zone():
    player_units = make_unit_states(count=5)
    zone = get_deployment_zone(DeploymentType.STANDARD, 1, (60, 44))
    placements = place_units(player_units, deployment_type=DeploymentType.STANDARD,
                             player=1, map_size=(60, 44))
    for p in placements:
        assert zone.x_min <= p.x <= zone.x_max
        assert zone.y_min <= p.y <= zone.y_max


def test_melee_units_front():
    """Melee units размещаются ближе к врагу (передний край)."""
    player_units = make_unit_states(melee_count=2, ranged_count=2)
    placements = place_units(player_units, deployment_type=DeploymentType.STANDARD,
                             player=1, map_size=(60, 44))
    # Match melee/ranged back to their unit states by ID
    placement_map = {p.unit_id: p for p in placements}
    melee_placements = [placement_map[u.unit_id] for u in player_units
                        if _is_melee_unit(u)]
    ranged_placements = [placement_map[u.unit_id] for u in player_units
                         if _is_ranged_unit(u)]
    assert all(m.x > r.x for m, r in zip(melee_placements, ranged_placements))


def test_ranged_units_placed_in_cover():
    """Ranged units предпочитают клетки с cover."""
    ...


def test_warlord_placed_center():
    """Warlord размещается в центре deploy zone."""
    ...


def test_unknown_deployment_defaults_to_standard():
    try:
        zone = get_deployment_zone(DeploymentType.STANDARD, 1, (60, 44))
        assert isinstance(zone, DeploymentZone)
    except ValueError:
        pass  # Unknown type should raise — fallback handled by caller
```
