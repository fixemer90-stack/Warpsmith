---
title: Architecture (C4)
slug: architecture-c4
index: C4-L1–C4-L3
tags: [simulation, architecture]
status: accepted
date: 2026-04-30
edition: none
confidence: high
---

# Architecture (C4)

Симулятор сценариев Warhammer 40k — веб-приложение с FastAPI-бекендом, HTMX/Alpine-фронтом и JWT-авторизацией.

## Уровень 1 — Контекст

```mermaid
graph TD
    USER["👤 Пользователь"]
    BROWSER["🌐 Браузер"]
    APP["⚔️ Battle Simulator<br/>FastAPI Web App<br/>Auth + RBAC + Tiers"]
    WIKI["📚 Wiki Vault<br/>126 .md файлов"]
    DB["🗄 SQLite<br/>users (bcrypt),<br/>ростера, реплеи,<br/>подписки, платежи"]
    STRIPE["💳 Stripe<br/>Checkout + Webhooks<br/>+ Customer Portal"]
    OAUTH["🔑 OAuth Providers<br/>Google, VK, etc."]

    USER -->|HTMX + Alpine.js| BROWSER
    BROWSER -->|HTTP + httponly cookie| APP
    APP -->|парсинг юнитов| WIKI
    APP -->|SQL| DB
    APP -->|webhook| STRIPE
    APP -->|OAuth 2.0 redirect| OAUTH
```

## Уровень 2 — Контейнеры

```mermaid
graph TB
    subgraph Browser["🌐 Браузер"]
        HTML["Jinja2 HTML"]
        HTMX["HTMX (partial updates)"]
        ALPINE["Alpine.js (state)"]
        CANVAS["Canvas (2D map)"]
        STORAGE["localStorage<br/>(черновики)"]
    end

    subgraph Backend["⚙️ FastAPI Backend"]
        AUTH["🔐 Auth Module<br/>Authentication + Authorization<br/>register/login/logout/me<br/>OAuth via providers<br/>RBAC по user_id + is_public"]
        ROUTES["HTML Routes<br/>(pages)"]
        API["JSON API<br/>(/api/me, /api/rosters,<br/>/api/simulate)"]
        BILL["💳 Billing Module<br/>Stripe checkout + webhook<br/>+ customer portal"]
        FG["🎯 Feature Gate<br/>UserFeatures.for_user()<br/>— лимиты + гейты"]
        ADS["📢 Ad Injector<br/>серверная вставка рекламы<br/>для Free-tier"]
        GL["Game Loop"]
        AI["AI Engine"]
        CE["Combat Engine"]
        WL["Wiki Loader"]
    end

    WIKI["📚 Wiki Vault"]
    DB["🗄 SQLite"]
    STRIPE["💳 Stripe"]
    OAUTH["🔑 Google / VK / etc."]

    subgraph DB_Tables["Data Model"]
        USERS["users: id, email, pw_hash, display_name,<br/>tier, subscription_id, expires_at"]
        OAUTH_T["oauth_accounts: provider,<br/>provider_user_id, user_id,<br/>access_token"]
        ROST["rosters: id, user_id, name, faction, pts, units(JSON),<br/>is_public — автор/гость/публичный"]
        SCEN["scenarios: id, user_id, roster_a, roster_b, result"]
        REPL["replays: id, scenario_id, round, phase, events(JSON)"]
        SUBS["subscriptions: id, user_id, stripe_sub_id,<br/>tier, status, expires_at"]
        PAY["payments: id, user_id, amount,<br/>currency, status, stripe_pi_id"]
    end

    HTML -->|GET/POST| ROUTES
    AUTH -->|register/login/logout| DB
    API -->|get_current_user ← cookie| AUTH
    API -->|authorize: свои записи / публичные| AUTH
    API -->|feature gate: tier check| FG
    API -->|CRUD| DB
    GL --> DB
    BILL -->|/api/subscribe| STRIPE
    BILL -->|/api/webhooks/stripe| STRIPE
    BILL -->|сохранить подписку| DB
    FG -->|free/premium dict| AUTH
    FG -->|роуты проверяют tier| API
    ADS -->|Free tier → ad banners| ROUTES
    AUTH -->|OAuth callback| OAUTH
```

## Уровень 3 — Компоненты

```mermaid
graph TB
    subgraph Auth["🔐 Auth"]
        REG["POST /auth/register"]
        LOGIN["POST /auth/login"]
        LOGOUT["GET /auth/logout"]
        ME["GET /api/me"]
        JWT_MW["get_current_user<br/>(cookie → JWT)"]
        SHA["SHA-256 hasher"]
        AUTHZ["Authorization Rules<br/>— user_id → свои записи<br/>— is_public → все<br/>— guest → user_id=1"]
        OAUTH_LOGIN["GET /auth/{provider}/login<br/>редирект на OAuth сервер"]
        OAUTH_CB["GET /auth/{provider}/callback<br/>code → token → user"]
        GOOG_P["GoogleProvider<br/>OIDC /userinfo"]
        VK_P["VKProvider<br/>OAuth 2.0 /users.get"]
    end

    subgraph Web["Web UI"]
        TB["Team Builder"]
        SS["Scenario Setup"]
        RV["Round Viewer"]
        PROF["My Rosters / Replays"]
    end

    subgraph API["API"]
        R_ROST["POST /api/rosters"]
        G_ROST["GET /api/rosters"]
        D_ROST["DELETE /api/rosters/{id}"]
        R_SIM["POST /api/simulate"]
        R_REP["GET /api/replays/{id}"]
    end

    subgraph Engine["Engine"]
        WL["Wiki Loader"]
        GL["Game Loop"]
        AI["AI Engine"]
    end

    subgraph DB["SQLite"]
        USERS_T["users"]
        ROST_T["rosters<br/>user_id + is_public"]
        SCEN_T["scenarios<br/>user_id"]
        REPL_T["replays<br/>через scenario_id"]
    end

    REG --> SHA --> USERS_T
    LOGIN --> SHA --> JWT_MW
    JWT_MW --> AUTHZ
    AUTHZ --> R_ROST
    AUTHZ --> G_ROST
    AUTHZ --> D_ROST
    JWT_MW --> ME
    ME --> USERS_T
    TB --> R_ROST --> AUTHZ
    TB --> |проверка лимита ростереров| FG
    R_ROST --> |max_rosters check| FG
    R_ROST --> ROST_T
    PROF --> G_ROST --> AUTHZ
    G_ROST --> |фильтр: user_id OR is_public| ROST_T
    D_ROST --> |только свой user_id| ROST_T
    R_SIM --> |premium?| FG
    R_SIM --> SCEN_T

    subgraph Billing["💳 Billing"]
        SUB["POST /api/subscribe<br/>создать Stripe Checkout"]
        WH["POST /api/webhooks/stripe<br/>обновить tier"]
        PORTAL["GET /api/billing/portal<br/>Stripe Customer Portal"]
        PLANS["Plans Config<br/>free / premium"]
    end

    subgraph Features["🎯 Feature Gate"]
        FG["UserFeatures.for_user()<br/>— лимиты + гейты"]
        REQ_TIER["require_tier('premium')<br/>FastAPI Depends"]
    end

    subgraph Ads["📢 Ad Injector"]
        AD_INJECT["Ad placeholder<br/>в base.html / страницах"]
        AD_VIEW["POST /api/ads/impression<br/>счётчик показов"]
    end

    SUB --> |redirect| STRIPE_EXT["💳 Stripe<br/>Checkout"]
    STRIPE_EXT --> |webhook event| WH
    WH --> |INSERT / UPDATE| SUBS_T["subscriptions"]
    WH --> |UPDATE tier| USERS_T
    PORTAL --> |redirect| STRIPE_CP["💳 Stripe<br/>Customer Portal"]
    PLANS --> FG
    FG --> |dict features| REQ_TIER
    FG --> |features → Alpine| ME
    AD_INJECT --> |для free-tier| PAGES["Pages"]
    AD_VIEW --> SUBS_T

    OAUTH_LOGIN --> |редирект| GOOG["🔑 Google OAuth"]
    OAUTH_LOGIN --> |редирект| VK["🔑 VK OAuth"]
    GOOG --> |code| OAUTH_CB
    VK --> |code| OAUTH_CB
    OAUTH_CB --> |создать/найти user| USERS_T
    OAUTH_CB --> |сохранить| OAUTH_T["oauth_accounts"]
    OAUTH_CB --> JWT_MW
```

## Структура проекта (web/)

```
web/
├── routes/
│   ├── __init__.py
│   ├── auth.py         ← /register, /login, /logout
│   ├── pages.py        ← HTML-страницы
│   └── api.py          ← JSON API (+ JWT middleware)
├── templates/
│   ├── base.html
│   ├── auth/
│   │   ├── login.html
│   │   └── register.html
│   └── ...
├── static/
│   ├── team_builder.js
│   └── icons/*.svg
```

## Схема БД

```sql
CREATE TABLE users (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    email        TEXT NOT NULL UNIQUE,
    password_hash TEXT NOT NULL,
    display_name TEXT DEFAULT '',
    created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login   TIMESTAMP
);

CREATE TABLE rosters (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id     INTEGER DEFAULT 1 REFERENCES users(id),
    name        TEXT NOT NULL,
    faction     TEXT NOT NULL,
    pts_limit   INTEGER DEFAULT 2000,
    detachment  TEXT,
    units       TEXT NOT NULL,   -- JSON
    is_public   BOOLEAN DEFAULT 0,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE scenarios (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id     INTEGER REFERENCES users(id),
    roster_a_id INTEGER REFERENCES rosters(id),
    roster_b_id INTEGER REFERENCES rosters(id),
    mission     TEXT,
    map         TEXT,             -- JSON (map config)
    result      TEXT,             -- JSON (final score)
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE replays (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    scenario_id INTEGER REFERENCES scenarios(id),
    round       INTEGER,
    phase       TEXT,
    events      TEXT NOT NULL,    -- JSON array
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS oauth_accounts (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER NOT NULL REFERENCES users(id),
    provider        TEXT NOT NULL,
    provider_user_id TEXT NOT NULL,
    access_token    TEXT,
    refresh_token   TEXT,
    token_expires_at TIMESTAMP,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(provider, provider_user_id)
);

CREATE TABLE IF NOT EXISTS subscriptions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER NOT NULL REFERENCES users(id),
    stripe_sub_id   TEXT NOT NULL,
    stripe_cust_id  TEXT,
    tier            TEXT NOT NULL DEFAULT 'premium',
    status          TEXT NOT NULL DEFAULT 'active',
    started_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at      TIMESTAMP,
    canceled_at     TIMESTAMP
);

CREATE TABLE IF NOT EXISTS payments (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER REFERENCES users(id),
    subscription_id INTEGER REFERENCES subscriptions(id),
    stripe_pi_id    TEXT,
    amount          INTEGER,            -- в центах
    currency        TEXT DEFAULT 'usd',
    status          TEXT,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## Правила авторизации (Authorization)

### Базовая (Across tiers)

| Операция | Условие доступа | Проверка |
|----------|----------------|----------|
| POST /api/rosters | Любой авторизованный пользователь | JWT → user_id → INSERT |
| GET /api/rosters | Только свои ростера | `WHERE user_id = current_user.id` |
| GET /api/rosters (публичные) | Любой (включая гостей) | `WHERE is_public = 1` |
| DELETE /api/rosters/{id} | Только автор ростера | `user_id == current_user.id` |
| POST /api/simulate | Любой авторизованный | user_id → запись в scenarios |
| GET /api/replays/{id} | Автор сценария или публичный | через scenario.user_id |
| GET /auth/me | Любой с JWT-токеном | decode → вернуть user |
| GET /auth/register/login | Все (включая гостей) | без проверки |
| Гостевой режим | Без JWT → user_id = 1 | только localStorage, нет DELETE |

### Tier-based (Subscription)

| Функция | Free | Premium | Проверка |
|---------|------|---------|----------|
| Max rosters | 1 | без лимита | `UserFeatures.for_user(user).max_rosters` |
| Simulation AI | basic | full | `require_tier("premium")` на эндпоинте |
| Export CSV/JSON | ❌ | ✅ | `require_tier("premium")` |
| Public rosters | смотреть | создавать | feature gate |
| Ads | показывать | скрыть | `user.features.ads_enabled` в шаблоне |
| Priority sim | очередь | мгновенно | `require_tier("premium")` |
